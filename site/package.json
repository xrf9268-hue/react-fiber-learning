{
  "name": "react-fiber-learning-site",
  "version": "1.0.0",
  "private": true,
  "description": "React Fiber Learning - aixie.de",
  "scripts": {
    "sync": "bash sync-content.sh",
    "dev": "bash sync-content.sh && vitepress dev",
    "build": "bash sync-content.sh && vitepress build",
    "preview": "vitepress preview",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13",
    "wrangler": "^4"
  }
}
